<clickhouse>
    <!-- Enable ClickHouse's built-in server-internal time-series so the Grafana
         CH-health panels have history (not just live snapshots). `replace="1"`
         overrides the image default outright. 14-day TTL keeps the *_log tables
         bounded at production ingest volume (5k+ replays/day) — these grow with
         server uptime, not data, so a short window is plenty for trend panels. -->
    <metric_log replace="1">
        <database>system</database>
        <table>metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <collect_interval_milliseconds>1000</collect_interval_milliseconds>
        <ttl>event_date + INTERVAL 14 DAY DELETE</ttl>
    </metric_log>
    <asynchronous_metric_log replace="1">
        <database>system</database>
        <table>asynchronous_metric_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
        <ttl>event_date + INTERVAL 14 DAY DELETE</ttl>
    </asynchronous_metric_log>
</clickhouse>
