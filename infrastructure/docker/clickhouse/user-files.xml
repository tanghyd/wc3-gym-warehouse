<?xml version="1.0"?>
<!--
  file() is chrooted to user_files_path. Compose mounts the parsed docs
  read-only at /app/data (${W3WAREHOUSE_DATA_DIR:-./data}), so with
  user_files_path at /app/ a repo-relative glob such as
  data/parsed/gnl/*.json resolves inside this server (just local::load).
-->
<clickhouse>
    <user_files_path>/app/</user_files_path>
</clickhouse>
